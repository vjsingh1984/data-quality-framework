= Databricks authentication and runtime compatibility

The Databricks integration uses the Databricks SDK's unified authentication.
For local development, use the Databricks CLI OAuth login. For unattended
workloads, prefer OAuth machine-to-machine credentials or workload identity
federation (WIF); do not commit personal access tokens.

For GitHub Actions WIF, configure the Databricks federation policy and expose
only `DATABRICKS_HOST`, `DATABRICKS_CLIENT_ID`, and
`DATABRICKS_AUTH_TYPE=github-oidc`. Construct `DatabricksCheck()` without a
token so the SDK obtains the short-lived credential. The legacy explicit-token
constructor remains supported for compatibility, passes the token directly to
the SDK, and does not retain it on the check object.

The optional Databricks integration requires Python 3.10 or newer. The core
package and Spark 3.5 integration continue to support Python 3.9.

Official references:

* https://docs.databricks.com/aws/en/dev-tools/auth/oauth-federation-provider[OAuth federation policy]
* https://docs.databricks.com/aws/en/dev-tools/ci-cd/github[GitHub Actions with workload identity federation]
* https://docs.databricks.com/aws/en/dev-tools/auth/unified-auth[Databricks unified authentication]

== Runtime boundary

Databricks Runtime 18 LTS uses Apache Spark 4.1, JDK 21, and Python 3.12.
This project's current PyDeequ 1.6.0 dependency maps Deequ artifacts only
through Spark 3.5. CI therefore verifies PySpark 3.5.9 with Java 11 and does not
claim Runtime 18 compatibility. A Spark 4.1 lane should be added only after an
upstream PyDeequ release maps and tests Spark 4.1; the current boundary is an
explicit compatibility hold, not an untested upgrade.

* https://docs.databricks.com/aws/en/release-notes/runtime/18[Databricks Runtime 18 LTS]
* https://github.com/awslabs/python-deequ/blob/v1.6.0/pydeequ/configs.py[PyDeequ 1.6.0 Spark mapping]
